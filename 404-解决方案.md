# Vercel 部署 404 问题解决方案

## 🔍 问题分析

当你部署到 Vercel 时遇到 404 错误，通常是因为：

1. **入口文件问题** - Vercel 找不到 `start.html`
2. **路由配置错误** - 请求被错误路由
3. **缓存问题** - Vercel 缓存了旧的配置
4. **文件路径问题** - 文件不在正确位置

## ✅ 解决方案

### 方案1：使用最简单的静态文件配置（推荐）

这是最简单的方法，不需要任何服务器代码。

#### 步骤：
1. **删除不需要的文件**
   ```bash
   # 删除 API 相关文件
   rm -rf api/
   rm -f vercel.json
   rm -f vercel-static.json
   rm -f vercel-simple.json
   rm -f vercel.config.js
   rm -f vercel-test.js
   rm -f check-files.js
   rm -f deploy.sh
   ```

2. **创建最简单的 vercel.json**
   ```json
   {
     "version": 2,
     "builds": [
       {
         "src": "**/*",
         "use": "@vercel/static"
       }
     ],
     "routes": [
       {
         "src": "/(.*)",
         "dest": "/$1"
       }
     ]
   }
   ```

3. **确保文件结构正确**
   ```
   fitness-website/
   ├── start.html          # 主入口（必须）
   ├── index.html          # 主界面
   ├── plan-guide.html     # 计划说明
   ├── css/
   │   └── style.css
   ├── js/
   │   ├── app.js
   │   └── i18n.js
   ├── data/
   │   └── plans.json
   └── vercel.json         # 配置文件
   ```

4. **部署到 Vercel**
   ```bash
   # 方法A：使用 Vercel CLI
   vercel --prod
   
   # 方法B：使用 GitHub
   # 1. 推送到 GitHub
   git add .
   git commit -m "Deploy to Vercel"
   git push origin main
   
   # 2. 在 Vercel 中导入仓库
   # 3. Vercel 会自动检测为静态网站
   # 4. 点击 "Deploy"
   ```

### 方案2：使用 Vercel 的默认配置

如果不想创建 vercel.json，Vercel 会自动检测静态网站。

#### 步骤：
1. **删除所有配置文件**
   ```bash
   rm -f vercel*.json
   rm -f vercel*.js
   rm -f api/ -rf
   ```

2. **确保 start.html 在根目录**
   ```bash
   # 检查文件位置
   ls -la start.html
   
   # 如果不在根目录，移动它
   # mv path/to/start.html .
   ```

3. **部署**
   ```bash
   vercel --prod
   ```

### 方案3：使用 GitHub Pages（备选方案）

如果 Vercel 持续出现问题，可以考虑使用 GitHub Pages。

#### 步骤：
1. **创建 GitHub 仓库**
2. **推送代码**
   ```bash
   git add .
   git commit -m "Initial commit"
   git push origin main
   ```

3. **启用 GitHub Pages**
   - 在 GitHub 仓库设置中
   - 点击 "Pages"
   - 选择 "main" 分支
   - 点击 "Save"

4. **访问网站**
   - 等待几分钟
   - 访问 `https://你的用户名.github.io/仓库名/`

## 🛠️ 故障排除

### 问题1：访问 `https://your-project.vercel.app/start.html` 显示 404

**可能原因：**
1. `start.html` 文件不存在
2. 文件路径错误
3. Vercel 配置错误

**解决方案：**
1. 检查文件是否存在：
   ```bash
   ls -la start.html
   ```

2. 检查文件路径：
   ```bash
   # 确保在项目根目录
   pwd
   ```

3. 检查 Vercel 配置：
   ```bash
   cat vercel.json
   ```

### 问题2：访问 `https://your-project.vercel.app/` 显示 404

**可能原因：**
1. Vercel 没有将根路径重定向到 start.html
2. 配置文件错误

**解决方案：**
1. 创建正确的 vercel.json：
   ```json
   {
     "version": 2,
     "builds": [
       {
         "src": "**/*",
         "use": "@vercel/static"
       }
     ],
     "routes": [
       {
         "src": "/",
         "dest": "/start.html"
       },
       {
         "src": "/(.*)",
         "dest": "/$1"
       }
     ]
   }
   ```

2. 重新部署：
   ```bash
   vercel --prod
   ```

### 问题3：页面显示错误但不是 404

**可能原因：**
1. CSS/JS 文件加载失败
2. 路径错误
3. 缓存问题

**解决方案：**
1. 检查浏览器控制台错误
2. 清除浏览器缓存
3. 检查文件路径是否正确

## 📋 部署检查清单

### 部署前检查
- [ ] `start.html` 文件存在
- [ ] `start.html` 在项目根目录
- [ ] 所有 CSS 文件存在
- [ ] 所有 JavaScript 文件存在
- [ ] `vercel.json` 配置正确（如果使用）

### 部署后检查
- [ ] 访问部署后的 URL
- [ ] 检查是否显示 404
- [ ] 检查浏览器控制台错误
- [ ] 测试所有功能

## 🚀 快速部署步骤（推荐）

### 步骤1：清理项目
```bash
# 删除不需要的文件
rm -rf api/
rm -f vercel*.json
rm -f vercel*.js
rm -f check-files.js
rm -f deploy.sh
rm -f 404-解决方案.md
rm -f 部署检查清单.md
rm -f 项目总结.md
rm -f VERCEL_DEPLOYMENT.md
rm -f DEPLOYMENT.md
rm -f 使用说明.md
rm -f README.md
rm -f 项目总结.md
rm -f 部署检查清单.md
```

### 步骤2：创建最简单的配置
```bash
# 创建 vercel.json
cat > vercel.json << 'EOF'
{
  "version": 2,
  "builds": [
    {
      "src": "**/*",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
EOF
```

### 步骤3：部署
```bash
# 方法A：使用 Vercel CLI
vercel --prod

# 方法B：使用 GitHub
git add .
git commit -m "Deploy to Vercel"
git push origin main
```

### 步骤4：测试
1. 访问 Vercel 提供的 URL
2. 如果显示 404，访问 `/start.html`
3. 如果仍然 404，检查 Vercel 部署日志

## 🎯 成功标准

部署成功后，你应该能够：
1. 访问 `https://your-project.vercel.app/start.html`
2. 看到启动页面
3. 点击"开始训练"进入主界面
4. 所有功能正常工作

## 📞 支持

如果问题仍然存在，请提供：
1. Vercel 部署日志
2. 浏览器控制台错误
3. 你的 vercel.json 配置
4. 项目文件结构

**祝你部署成功！💪**